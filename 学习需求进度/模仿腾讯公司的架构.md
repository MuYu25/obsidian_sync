- [x] polarisMesh已经接入k8s
- [x] go也使用SDK介入polaris
- [x] 将go项目打包成为docker image运行。
- [x] polaris-controller同步到polaris-console上
- [x] 将go项目的pod状态同步到polaris-console上.26-06-22 23:06,
解决办法：学习了一下使用 `helm`进行部署，将`address`,`token`与operator`填充完整后即可。