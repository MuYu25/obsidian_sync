## **今天完成了什么？值得肯定的地方是什么？**（目的：建立信心，看到进步，而不是只盯着没做完的事）
- 今天把昨天那个并发问题给解决了。
## **今天有什么事情可以做得更好？卡点在哪里？**（目的：发现问题，但不是自责。例如：“沟通时没先确认对方需求，导致返工”）
- 并发所导致的问题是前端的问题，他没在新增或者删除后的回调中去刷新列表，直接新增或者删除后，立马就去查询数据库了。我怎么做我tm自己死都测试不出来。
```go
func main() {
    // 示例：GET 请求
    // if err := doGetExample(); err != nil {
    //  fmt.Println("GET 请求失败:", err)
    // }
    queryCh := make(chan struct{}, 1)
    ch := make(chan struct{}, 1)
    go func(<-chan struct{}) {
        <-queryCh
        doGetExample("http://192.168.1.120:8000/soft/getLinkLst?page=1&pageSize=100")
	        ch <- struct{}{}
    }(queryCh)
    doPostExample("http://192.168.1.120:8000/soft/addLink", queryCh)
    // doPostDel("http://192.168.1.120:8000/soft/delLink", queryCh)
    <-ch
    // doGetExample("http://192.168.1.120:8000/soft/getLinkLst?page=1&pageSize=100")

}
```
因为我一直保持的有序，他自己写的两个都可能一起操作。纯并发。不一定有序。
## **基于以上，我明天可以立即尝试的一个小改进是什么？**（目的：将复盘转化为行动。例如：“明天开会前，先花2分钟写下核心目标和问题”）
- 下次再出现，请找前端的问题，前端是再怎样都能够等得了接口返回结果。浪费自己太多时间了。