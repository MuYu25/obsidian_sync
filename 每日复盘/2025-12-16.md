## **今天完成了什么？值得肯定的地方是什么？**（目的：建立信心，看到进步，而不是只盯着没做完的事）
- 今天完成了一个分页查询的随机排序，就是在加了`order by rand()`,难崩，有点逆天。而且还是存在问题，就是不同的页数中，可能会存在相同的数据。如果后续需要从中去重，可能就需要用到伪随机了。
- 今天还排查到了一些`clickhouse`不能通过网络连接的原因。我估计还是默认用户的问题。
## **今天有什么事情可以做得更好？卡点在哪里？**（目的：发现问题，但不是自责。例如：“沟通时没先确认对方需求，导致返工”）
- 前端`get`请求，某一个值为`null`时，还是会传入这个对象，如果后台使用指针去接受，那么指针将不会是空值。留意一下，记住一下，后续避免返工。
- 昨天干了一件很蠢的事情，把今天的自己给坑了。强制执行`git reset`了，还好仓库能看到旧更改，妈的。不然坑死自己，代码都得重写。这件事是这样的，test分支没合并最新的dev分支，我的新改动合并了新的dev分支。就导致了冲突，然后我去由于我是先合并的dev再添加的新内容。就想着回滚到合并dev之前，就reset。reset后导致不能使用`cherry-pick`，不能把新的改动给搞出来。真的der，下次这种情况直接`git checkout {{commit}}`也可以，直接`reset`真的太暴力了。把自己的坑的半死
- 今天排查`clickhouse`不能通过网络连接的原因，问了很久`AI`，但作用其实不是很明显，而且优势新部署，直接看文档就好了。问`AI`花的时间还多。而且还不值得。
## **基于以上，我明天可以立即尝试的一个小改进是什么？**（目的：将复盘转化为行动。例如：“明天开会前，先花2分钟写下核心目标和问题”）
- `get`请求不要使用指针去区分是否传入某个值，没用。还是必须得使用类型的默认值判断才行。
- 所有新的改动全部在新的分支上进行，并且从新分支合并到`dev`上,不然出现这种存在多个`merge request`的情况下，要被坑死。
- 还有少用`reset`，再多用打死。
- 部署和搞新的应用或者中间件，先看文档，直接看`dockerhub`的文档就好了。然后配置一下`docker-compose`我觉得差不多就搞定了。